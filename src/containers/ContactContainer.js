import { connect } from "react-redux";
import ContactView from "../components/contact/ContactView"

// const mapStateToProps = (state) => ({})
const container = connect(null, null)

export default container(ContactView)